@model IEnumerable<bibliotecaCrud.Models.Autor>

@{
    ViewData["Title"] = "Index";
}

<h1>Autores</h1>

<p>
    <a asp-action="Create">Cadastrar Novo Autor</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nome)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Nacionalidade)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Nacionalidade)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdAutor">Editar</a> |
                <a asp-action="Details" asp-route-id="@item.IdAutor">Detalhes</a> |
                <a asp-action="Delete" asp-route-id="@item.IdAutor">Deletar</a>
            </td>
        </tr>
}
    </tbody>
</table>
